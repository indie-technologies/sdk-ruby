source 'https://rubygems.org'

git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gem 'rutie', github: 'MelianLabs/rutie', glob: 'gem/*.gemspec', submodules: true, branch: 'segmentation_fault'

gemspec
